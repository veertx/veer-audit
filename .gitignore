# Dependencies
node_modules/

# Scan outputs and generated reports — never commit these.
results/
reports/

# Real configs stay private; only the *.example.json files are committed.
config/target.json
config/triage.json

# Keep the directory placeholders.
!results/.gitkeep
!reports/.gitkeep

# OS / editor cruft
.DS_Store
Thumbs.db
*.log
